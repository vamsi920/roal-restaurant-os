import { HOME_CTA } from "@/lib/landing/home-theme";

export const HOME_CTA_BAND = {
  title: "Let the next busy call become an order.",
  description: "Hear the agent, load one menu, and decide if ROAL is ready before you forward real guest calls.",
  primary: HOME_CTA.primary,
  secondary: HOME_CTA.secondary,
} as const;
