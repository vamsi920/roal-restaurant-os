import { HOME_CTA } from "@/lib/landing/home-theme";

export const HOME_CTA_BAND = {
  title: "Turn the next missed call into an order.",
  description: "Live menu on every call. Kitchen tickets on your pass. You pay only when pickup confirms.",
  primary: HOME_CTA.primary,
  secondary: HOME_CTA.secondary,
} as const;
