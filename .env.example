# Copy to `.env` in the project root (see README). Next.js loads `.env` automatically.
# If you also have `.env.local`, it overrides duplicate keys—delete `.env.local` to rely only on `.env`.
NEXT_PUBLIC_SUPABASE_URL=https://mnkabwcbdxruefzuvuuv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1ua2Fid2NiZHhydWVmenV2dXV2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI4MTg5ODYsImV4cCI6MjA4ODM5NDk4Nn0.3kn1-SQybvweSeuqVwl7lh1WkSGT0qWNAtIdcx-nU0A
# Server-only: menu clear + KDS phone-order reads use this when set (bypasses RLS). From Supabase Dashboard → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash

# ElevenLabs Conv AI (server-only; never NEXT_PUBLIC_)
ELEVENLABS_API_KEY=
# Optional default for GET /api/integrations/elevenlabs/agent without ?agent_id=
ELEVENLABS_AGENT_ID=
# Same bearer secret as Supabase Edge (get-menu, sync-draft-order, finalize-order)
AGENT_TOOL_SECRET=
# Optional: require Authorization: Bearer <this> for POST .../sync-roal-tools
# ELEVENLABS_SYNC_TOKEN=
# IANA timezone for agent clock context (e.g. America/Chicago)
# RESTAURANT_AGENT_TIMEZONE=
# Set to 0 to skip creating/linking ROAL_order_taker_playbook KB doc
# ROAL_ORDER_KB=
